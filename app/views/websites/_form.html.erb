<%= form_with(model: website) do |form| %>
  <% if website.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(website.errors.count, "error") %> prohibited this website from being saved:</h2>

      <ul>
        <% website.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :url, style: "display: block" %>
    <%= form.text_field :url %>
  </div>

  <div>
    <%= form.label :restricted, style: "display: block" %>
    <%= form.check_box :restricted %>
  </div>

  <div>
    <%= form.label :timeout, style: "display: block" %>
    <%= form.number_field :timeout %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
